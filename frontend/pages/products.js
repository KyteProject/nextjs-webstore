import React from 'react';
import ProductList from './../containers/ProductList';

const Products = () => {
	return (
		<React.Fragment>
			<ProductList />
		</React.Fragment>
	);
};

export default Products;
